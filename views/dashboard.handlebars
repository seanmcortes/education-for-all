<!-- 
Program: Education For All
Authors: Jason Anderson, Sean Cortes, Joel Huffman, Tingting Lin, Ting Ju Sheppy
Concept: Jacob Carter
-->
<!DOCTYPE html>
<html>
  <head>
    <link href="dashboard.css" rel="stylesheet" type="text/css">
    <meta charset="UTF-8">
  </head>
  <body>
    
    <div class="container custom-container">
      <div class="row">
        {{> navbar }}
      </div>
      <div class="row">
        <div class="col-lg" id="banner-image"></div>
      </div>
      <div class="row">
        <div class="col-lg" id="banner-course">
          <div class="banner-course-container">
            <h1>COURSES</h1>
            <p>Select a course you are enrolled in to view lectures and submit assignments<br>
              Let's get studying!</p>
          </div>
        </div>
      </div>
      <div class="row">
        <div class="card-container container-fluid">
          <div class="d-flex flex-row">
            {{#if courseLink}}
              {{#each courseLink}}
                <div class="card">
                  <img class="card-img-top" src="images/{{this.name}}.jpeg">
                  <div class="card-body bg-dark">
                    <a href="/course/{{this.course_id}}" id={{this.id}}>
                      <h5 class="card-text text-center">{{this.name}}</h5>
                    </a>
                  </div>
                </div>
              {{/each}}
            {{/if}}
          </div>
        </div>
      </div> 
    </div>
    <script type="text/javascript" src="/client.js"></script>
  </body>
</html>
